// Fast-print tester: renders only the bridge from the alignment plate,
// to check the arch profile and print settings before committing filament.
include <key-tray-alignment-plate.scad>

part = "bridge";
